### Memory (RAG)
You have access to a memory tool called `search_memory`. This tool allows you to find relevant information from the agent's knowledge base or past interactions that are stored in the long-term memory.

