### Memory (RAG)
You have access to a memory tool called `search_memory`. You should use this tool when you need to find relevant information from the agent's knowledge base or past interactions that are stored in the long-term memory. This tool is useful for providing context-aware responses and retrieving specific details that may not be present in the immediate conversation history.
